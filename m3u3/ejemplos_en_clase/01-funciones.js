//una funcion es un conjunto de instrucciones que se agrupan para realizar una tarea concreta y que se pueden reutilizar facilmente.

function suma(a,b){
    return a + b
}

const resultado = suma(10,5)
console.log(resultado)